; this script is generated automatically

DESCRIPTION: 
theophylline_project.mlxtran

DATA: 
    path = "%MLXPROJECT%/",
    file  ="theophylline_data.txt",
    headers = {ID,AMT,IGNORE,TIME,Y,COV,IGNORE},
    columnDelimiter = "\t"

VARIABLES: 
    WEIGHT,
    lw70 = log(WEIGHT/70) [use=cov]

INDIVIDUAL: 
    Cl = {distribution=logNormal, covariate=lw70, iiv=yes},
    V = {distribution=logNormal, covariate=lw70, iiv=yes},
    ka = {distribution=logNormal, iiv=yes}

STRUCTURAL_MODEL: 
    file = "mlxt:theophylline_mlxt",
    path = "%MLXPROJECT%",
    output = {Cc}


OBSERVATIONS: 
    y1 = {type=continuous, prediction=Cc, error=proportional}

TASKS: 
    ; settings
    globalSettings={
        withVariance=no,
        settingsAlgorithms="%MLXPROJECT%/theophylline_project_algorithms.xmlx",
        resultFolder="%MLXPROJECT%/theophylline_project"},
    ; workflow
    estimatePopulationParameters(
        initialValues={
            pop_Cl = 1,
            beta_{Cl,lw70} = 0 ,
            pop_V = 20,
            beta_{V,lw70} = 0 ,
            pop_ka = 1,
            b_y1 = 0.3,
            omega_Cl = 1,
            omega_V = 1,
            omega_ka = 1
            } ),
    estimateFisherInformationMatrix( method={stochasticApproximation} ),
    estimateIndividualParameters( method={conditionalMode} ),

